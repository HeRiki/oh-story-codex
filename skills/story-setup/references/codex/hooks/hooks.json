{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "FOUND=; for PYBIN in python3 python py; do \"$PYBIN\" -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1)\" >/dev/null 2>&1 && { FOUND=1; break; }; done; [ -n \"$FOUND\" ] || exit 0; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; case \"$PROJECT_ROOT\" in \"\"|/) exit 0;; esac; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" session-start",
            "timeout": 10,
            "statusMessage": "Loading story context",
            "commandWindows": "python -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" session-start 2>nul || py -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" session-start 2>nul || python3 -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" session-start 2>nul || exit /b 0"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|shell_command|functions.shell_command|apply_patch|functions.apply_patch|Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FOUND=; for PYBIN in python3 python py; do \"$PYBIN\" -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1)\" >/dev/null 2>&1 && { FOUND=1; break; }; done; [ -n \"$FOUND\" ] || exit 0; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; case \"$PROJECT_ROOT\" in \"\"|/) exit 0;; esac; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" pre-tool-prose-guard",
            "timeout": 10,
            "statusMessage": "Checking story outline guard",
            "commandWindows": "python -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" pre-tool-prose-guard 2>nul || py -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" pre-tool-prose-guard 2>nul || python3 -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" pre-tool-prose-guard 2>nul || exit /b 0"
          }
        ]
      },
      {
        "matcher": "Bash|shell_command|functions.shell_command",
        "hooks": [
          {
            "type": "command",
            "command": "FOUND=; for PYBIN in python3 python py; do \"$PYBIN\" -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1)\" >/dev/null 2>&1 && { FOUND=1; break; }; done; [ -n \"$FOUND\" ] || exit 0; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; case \"$PROJECT_ROOT\" in \"\"|/) exit 0;; esac; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" pre-tool-commit-advisory",
            "timeout": 15,
            "statusMessage": "Checking story commit warnings",
            "commandWindows": "python -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" pre-tool-commit-advisory 2>nul || py -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" pre-tool-commit-advisory 2>nul || python3 -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" pre-tool-commit-advisory 2>nul || exit /b 0"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "manual|auto",
        "hooks": [
          {
            "type": "command",
            "command": "FOUND=; for PYBIN in python3 python py; do \"$PYBIN\" -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1)\" >/dev/null 2>&1 && { FOUND=1; break; }; done; [ -n \"$FOUND\" ] || exit 0; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; case \"$PROJECT_ROOT\" in \"\"|/) exit 0;; esac; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" pre-compact",
            "timeout": 10,
            "statusMessage": "Summarizing story context",
            "commandWindows": "python -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" pre-compact 2>nul || py -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" pre-compact 2>nul || python3 -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" pre-compact 2>nul || exit /b 0"
          }
        ]
      }
    ],
    "PostCompact": [
      {
        "matcher": "manual|auto",
        "hooks": [
          {
            "type": "command",
            "command": "FOUND=; for PYBIN in python3 python py; do \"$PYBIN\" -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1)\" >/dev/null 2>&1 && { FOUND=1; break; }; done; [ -n \"$FOUND\" ] || exit 0; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; case \"$PROJECT_ROOT\" in \"\"|/) exit 0;; esac; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" post-compact",
            "timeout": 10,
            "statusMessage": "Restoring story context hint",
            "commandWindows": "python -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" post-compact 2>nul || py -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" post-compact 2>nul || python3 -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" post-compact 2>nul || exit /b 0"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "FOUND=; for PYBIN in python3 python py; do \"$PYBIN\" -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1)\" >/dev/null 2>&1 && { FOUND=1; break; }; done; [ -n \"$FOUND\" ] || exit 0; PROJECT_ROOT=\"${CODEX_PROJECT_DIR:-}\"; if [ -z \"$PROJECT_ROOT\" ] || [ ! -f \"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\" ]; then SEARCH_DIR=\"$PWD\"; while [ \"$SEARCH_DIR\" != \"/\" ] && [ ! -f \"$SEARCH_DIR/.codex/hooks/story_codex_hook.py\" ]; do SEARCH_DIR=\"$(dirname \"$SEARCH_DIR\")\"; done; PROJECT_ROOT=\"$SEARCH_DIR\"; fi; case \"$PROJECT_ROOT\" in \"\"|/) exit 0;; esac; HOOK=\"$PROJECT_ROOT/.codex/hooks/story_codex_hook.py\"; [ -f \"$HOOK\" ] || exit 0; CODEX_PROJECT_DIR=\"$PROJECT_ROOT\" \"$PYBIN\" \"$HOOK\" stop",
            "timeout": 5,
            "commandWindows": "python -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" stop 2>nul || py -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" stop 2>nul || python3 -c \"import sys,operator; operator.lt(sys.version_info[:2], (3, 10)) and sys.exit(1); import os,runpy; from pathlib import Path; env=os.environ.get('CODEX_PROJECT_DIR'); starts=([Path(env).resolve()] if env else [])+[Path.cwd().resolve()]; root=next((p for s in starts for p in [s,*s.parents] if (p/'.codex'/'hooks'/'story_codex_hook.py').is_file()), None); sys.exit(0) if root is None else None; hook=root/'.codex'/'hooks'/'story_codex_hook.py'; os.environ['CODEX_PROJECT_DIR']=str(root); sys.argv=[str(hook), sys.argv[1]]; runpy.run_path(str(hook), run_name='__main__')\" stop 2>nul || exit /b 0"
          }
        ]
      }
    ]
  }
}
